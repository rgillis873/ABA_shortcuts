doctype html
html
  head
    title Maximum Aggregate Area
    include ./navbar.pug
    script(src="https://unpkg.com/xlsx/dist/xlsx.full.min.js")
    script(src='./js/upo_charts.js')
    script(src='./js/aggregate_area.js')

  body
    style.
      .table{
        width: 80%;
        margin-top: 1%;
        margin-right: 10%;
        margin-bottom: 10%;
        margin-left: 10%;
        border-collapse: separate!important;
      }
      div {
        text-align: center;  
      }
      ul {
        list-style-type: none;
      }
    div
      h1 Calculate Maximum Aggregate Area of Unprotected Openings in Exterior Walls
      h4 *Disclaimer* 
      p This page is to be used to aid in the calculation of Maximum Aggregate Area of Unprotected Openings. It is the user's responsibility to verify that the calculated values are correct.
      strong Allows for quick calculations of Maximum Aggregate Area of Unprotected Openings using the following table:
      ul
        li Table 9.10.14.4. Maximum Aggregate Area of Unprotected Openings in Exterior Walls
      strong Choose building type:
      br
      select.form-select(aria-label='Default select example' id='buildingType')
        option(value='C') C
        option(value='D') D
        option(value='E') E
        option(value='F1') F1
        option(value='F2') F2
        option(value='F3') F3
      br
      br
      strong Upload excel file with measurements
      form(enctype="multipart/form-data")
        div
          input(type="file" id="excelFile" onclick="this.value=null;" accept=".xls,.xlsx")
      table(id="myTable" class="table table-sm table-bordered")